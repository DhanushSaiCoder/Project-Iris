{
    "private": true,
    "workspaces": [
        "backend",
        "frontend"
    ],
    "devDependencies": {
        "concurrently": "^9.2.0"
    },
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run start --workspace=frontend\"",
        "start": "concurrently \"npm start --workspace=backend\" \"npm run start --workspace=frontend\""
    },
    "dependencies": {
        "jwt-decode": "^4.0.0"
    }
}
